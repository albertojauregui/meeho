<h1>Register to <%= @event.name %></h1>
<table>
  <thead>
    <tr>
      <th>Email</th>
    </tr>
  </thead>
  <tbody>    
    <% @users_of_events.each do |user| %>
        <tr><%= user.email %></tr>
    <% end %>
  </tbody>
</table>
<%= form_tag controller: "events", action: "register_user", method: :post do %>
<%= label_tag "Email" %>
<%= text_field_tag :email %>

<%= submit_tag "Registrar usuario" %>
<% end  %>
